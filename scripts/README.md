# 📋 Скрипты для заполнения данных

В этой папке находятся вспомогательные скрипты для инициализации данных в боте Pilates Reformer.

## 📚 populate_faq.py

**Назначение:** Заполняет лист "FAQ" в Google Sheets часто задаваемыми вопросами.

**Использование:**
```bash
python scripts/populate_faq.py
```

**Требования:**
- Переменная окружения `GOOGLE_SHEET_ID` в `.env`
- Файл `service_account.json` с креденшалами Google в корневой папке
- Доступ к Google Sheets API

**Что делает:**
1. Подключается к Google Sheets по ID из конфига
2. Ищет лист "FAQ" (создаёт, если его нет)
3. Очищает существующие данные (оставляет заголовок)
4. Добавляет 10 FAQ записей:
   - Что такое пилатес на реформере?
   - Сколько человек в группе?
   - Типы занятий и абонементов
   - Как записаться через бот?
   - Правила отмены и переноса
   - Срок действия абонемента
   - Способы оплаты
   - Что взять на тренировку?
   - Противопоказания
   - Напоминания бота

**Вывод:**
```
🚀 Запуск скрипта для заполнения FAQ в Google Sheets...

✅ Лист 'FAQ' найден
🗑️  Очищены существующие данные (оставлен заголовок)

📝 Добавляем 10 вопросов и ответов...

1. ✅ Что такое пилатес на реформере и подойдёт ли...
2. ✅ Сколько человек в группе и можно ли...
...

🎉 Успешно! Добавлено 10 FAQ записей в Google Sheets
📊 Всего строк в листе: 11
```

---

## 🗄️ populate_faq_local.py

**Назначение:** Заполняет локальную БД (SQLite) FAQ данными.

**Использование:**
```bash
python scripts/populate_faq_local.py
```

**Требования:**
- SQLite БД (`pilates_bot.db`)
- Асинхронные драйверы уже установлены в requirements.txt

**Что делает:**
1. Создаёт таблицу `faq` в локальной БД (если её нет)
2. Очищает существующие FAQ записи
3. Добавляет 10 новых FAQ записей
4. Сохраняет время добавления (поле `created_at`)

**Вывод:**
```
🚀 Запуск скрипта для заполнения FAQ в локальной БД...

✅ Таблица 'faq' проверена/создана в SQLite

🗑️  Удалено 0 старых FAQ записей

📝 Добавляем 10 вопросов и ответов...

1. ✅ Что такое пилатес на реформере и подойдёт ли...
...

🎉 Успешно! Добавлено 10 FAQ записей в локальную БД (SQLite)
```

---

## 🎯 Рекомендуемый порядок использования

### Для production (с Google Sheets):
```bash
# 1. Убедитесь, что .env содержит GOOGLE_SHEET_ID и GOOGLE_SERVICE_ACCOUNT_FILE
# 2. Убедитесь, что service_account.json находится в корневой папке
# 3. Запустите скрипт
python scripts/populate_faq.py
```

### Для локальной разработки:
```bash
# Опционально, если хотите протестировать FAQ локально
python scripts/populate_faq_local.py
```

---

## ✅ Проверка работы

После запуска скрипта FAQ будут доступны в боте:

1. **Через команду /faq:**
   ```
   /faq → показывает кнопки с вопросами
   Клиент нажимает на вопрос → видит ответ
   ```

2. **В Google Sheets:**
   - Откройте Google Sheet по ID из .env
   - Перейдите на лист "FAQ"
   - Проверьте, что все 10 вопросов и ответов добавлены

---

## 🔧 Структура данных FAQ

**Google Sheets (лист "FAQ"):**
| Вопрос | Ответ |
|--------|-------|
| Что такое пилатес на реформере? | (полный ответ) |
| Сколько человек в группе? | (полный ответ) |
| ... | ... |

**Источник в коде:**
```python
FAQ_DATA = [
    {
        "Вопрос": "...",
        "Ответ": "..."
    },
    ...
]
```

---

## 📝 Как добавить новые FAQ

1. **Отредактируйте массив `FAQ_DATA`** в скрипте:
```python
FAQ_DATA = [
    # Существующие FAQ...
    
    # Добавьте новый вопрос
    {
        "Вопрос": "Ваш новый вопрос?",
        "Ответ": "Полный ответ на вопрос..."
    }
]
```

2. **Запустите скрипт заново:**
```bash
python scripts/populate_faq.py
```

3. **FAQ обновится в Google Sheets**, и бот сразу подхватит новые вопросы!

---

## 🐛 Troubleshooting

### Ошибка: "Файл креденшалов 'service_account.json' не найден"
**Решение:**
```bash
# Скопируйте пример конфигурации
cp service_account.json.example service_account.json

# Заполните реальными креденшалами от Google Cloud Console
```

### Ошибка: "Лист 'FAQ' не найден"
**Решение:** Скрипт автоматически создаст лист "FAQ". Если ошибка повторяется, проверьте GOOGLE_SHEET_ID в .env.

### Бот не показывает новые FAQ
**Решение:** Перезагрузите бота после запуска скрипта:
```bash
# Если бот запущен в фоне
pkill -f "python main.py"

# Запустите снова
python main.py
```

---

## 📞 Документация

- **FAQ в боте:** Доступны в команде `/faq`
- **Код чтения FAQ:** `services/google_sheets.py` → функция `get_faq_answers()`
- **Маршрут FAQ:** `routers/faq.py`

