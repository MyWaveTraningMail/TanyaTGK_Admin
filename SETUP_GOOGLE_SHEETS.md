# 📋 Настройка Google Sheets для бота Pilates Reformer

## 🔴 КРИТИЧЕСКАЯ ОШИБКА КОНФИГУРАЦИИ

Бот обнаружил, что `GOOGLE_SHEET_ID` в файле `.env` содержит **тестовый ID вместо реального**.

```
GOOGLE_SHEET_ID=1aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789  ❌ НЕПРАВИЛЬНО (тестовый ID)
```

Это приводит к ошибкам:
```
ERROR - Google Sheet с ID '1aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789' не найден
ERROR - Ошибка логирования действия: <Response [404]>
ERROR - Ошибка чтения тренеров из Google Sheets: <Response [404]>
```

---

## ✅ Решение: Установка реального Google Sheet ID

### Шаг 1: Создайте новую Google Sheets таблицу

1. Перейдите на https://sheets.google.com/create
2. Дайте таблице название, например: `"Pilates Reformer Schedule"`
3. Создайте таблицу

### Шаг 2: Получите ID таблицы

1. Откройте созданную таблицу
2. Посмотрите на URL в адресной строке:
   ```
   https://docs.google.com/spreadsheets/d/ЗДЕСЬ_ВАШ_ID/edit#gid=0
                                        ↓
                              Скопируйте этот ID
   ```
3. Скопируйте ID (это очень длинная строка вроде `1q2w3e4r5t6y7u8i9o0p-1a2s3d4f5g6h7j8k9l`)

### Шаг 3: Обновите `.env` файл

Замените тестовый ID на реальный:

```bash
# ❌ БЫЛО (тестовый ID):
GOOGLE_SHEET_ID=1aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789

# ✅ ДОЛЖНО БЫТЬ (ваш реальный ID):
GOOGLE_SHEET_ID=1q2w3e4r5t6y7u8i9o0p-1a2s3d4f5g6h7j8k9l
```

### Шаг 4: Создайте листы в таблице

Таблица должна содержать 3 листа с правильными названиями:

1. **Schedule** (расписание)
   - Столбцы: Дата, Время, Тренер, Свободно, Цена, Типтренировки
   
2. **FAQ** (часто задаваемые вопросы)
   - Столбцы: Вопрос, Ответ
   
3. **Events** (логи действий)
   - Столбцы: Telegram ID, Время, Действие

### Шаг 5: Поделитесь доступом с сервисным аккаунтом

1. Откройте таблицу
2. Нажмите кнопку "Поделиться" (Share)
3. Добавьте email сервисного аккаунта из файла `service_account.json`
   - Найдите поле `"client_email"` в файле
   - Пример: `pilates-bot@project-id.iam.gserviceaccount.com`
4. Дайте права "Редактор" (Editor)
5. Отправьте ссылку

### Шаг 6: Перезапустите бот

```bash
python main.py
```

Если всё правильно настроено, вы должны увидеть:
```
INFO - Бот запущен и готов к работе
```

Вместо:
```
ERROR - Google Sheet с ID '...' не найден
```

---

## 🆘 Если ошибка продолжает появляться

1. **Проверьте, что ID скопирован полностью** (очень важно!)
2. **Убедитесь, что вы поделились доступом** с email сервисного аккаунта
3. **Проверьте файл `service_account.json`** - он должен быть валиден
4. **Пересоздайте таблицу** и повторите шаги

---

## 📞 Нужна помощь?

Если у вас остались вопросы, проверьте:
- Файл `.env` содержит GOOGLE_SHEET_ID
- Файл `service_account.json` находится в корне проекта
- Google Sheets таблица содержит листы: Schedule, FAQ, Events
- Сервисный аккаунт имеет доступ к таблице (роль: Редактор)

---

**Статус: ⏳ ОЖИДАНИЕ КОНФИГУРАЦИИ**

Бот функционирует, но Google Sheets интеграция недоступна. Установите реальный GOOGLE_SHEET_ID в `.env` файле.
